<?php

/**
 *
 * Contains messages list form html code
 *
*/

// we will handle actions with chats here
if (isset($_POST["action"])) {
	switch (strtolower($_POST["action"])) {
		default:
		break;
	}
}

$menu = '';

?>